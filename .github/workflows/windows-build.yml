name: Windows build

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        shell: pwsh
        run: .\build.ps1 -Version 3.0.12 -OutputDirectory .\dist

      - name: Run deterministic tests
        shell: pwsh
        run: |
          $exe = (Resolve-Path .\dist\NetOptimizer-v3.0.12.exe).Path
          $tests = @(
            '--self-test',
            '--failover-simulation',
            '--diagnostics-test',
            '--ui-layout-test',
            '--gui-startup-test'
          )
          foreach ($test in $tests) {
            $process = Start-Process -FilePath $exe -ArgumentList $test -Wait -PassThru -NoNewWindow
            if ($process.ExitCode -ne 0) {
              throw "Test failed: $test (exit code $($process.ExitCode))"
            }
          }

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
            name: NetOptimizer-v3.0.12-windows
          path: |
            dist/NetOptimizer-v3.0.12.exe
            dist/NetOptimizer-v3.0.12.pdb
